import Head from "next/head";
import Image from "next/image";
import Header from "../components/Header";
import { FaGithub } from "react-icons/fa";
import { FaGoogle } from "react-icons/fa";
import { FiChevronRight } from "react-icons/fi";

import Button from "../components/Button";

function Login() {
  return (
    <div className="flex min-h-screen flex-col bg-[#010410] font-author">
      <Head>
        <title>iconicnpm.com</title>
        <meta name="description" content="Generated by create next app" />
        /wikipedia/commons/a/a7/React-icon.svg
      </Head>
      <Header />

      <main className="mx-auto flex w-full flex-1 flex-col items-center justify-center font-author">
        <div className=" flex flex-col items-center justify-center space-y-6 rounded-xl bg-black/30 py-16 px-28">
          <Image src="/logo5.png" width={80} height={80} objectFit="contain" />

          <h2 className="text-2xl font-semibold text-white">
            Log in to Iconic
          </h2>
          <div className="flex space-x-4">
            <Button
              Icon={FaGithub}
              buttonColor="bg-[#010410]"
              buttonPadding="px-16"
            />
            <Button
              Icon={FaGoogle}
              buttonColor="bg-[#010410]"
              buttonPadding="px-16"
            />
          </div>

          <small className="text-lg font-medium text-gray-500">or</small>

          <form className="flex w-full max-w-xs flex-col" autoComplete="off">
            <div
              className={`headerLink gradientBorder rounded-md] mb-4 flex items-center justify-center p-[1.5px]`}
            >
              <input
                type="email"
                placeholder="Email address"
                className="w-full rounded-md bg-[#010410] px-4 py-3 outline-none"
              />
            </div>
            <div
              className={`headerLink gradientBorder rounded-md] mb-4 flex items-center justify-center p-[1.5px]`}
            >
              <input
                type="password"
                placeholder="Password"
                className="w-full rounded-md bg-[#010410] px-4 py-3 outline-none"
              />
            </div>
            <button className="mb-8 rounded-md bg-white/90 py-2 text-lg font-medium text-gray-800 transition duration-200 hover:bg-white/100 hover:text-black">
              Login
            </button>
            <Button
              text="Don’t have an account? Sign up"
              buttonColor="bg-[#010410]"
              Icon={FiChevronRight}
              translateX="translate-x-[25%]"
            />
          </form>
        </div>
      </main>
    </div>
  );
}

export default Login;
